# Database (MySQL)
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://taskpilot:password@localhost:3306/taskpilot"

# JWT — use a long random string in production
JWT_SECRET="replace-with-a-secure-random-secret"

# Next.js
NODE_ENV="production"
